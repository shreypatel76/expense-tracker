import {auth,db,googleProvider,signInWithPopup,signOut,onAuthStateChanged,doc,getDoc,setDoc,serverTimestamp} from "./firebase.js";
const loginBtn=document.getElementById("google-signin-btn");
if(loginBtn){loginBtn.addEventListener("click",async()=>{loginBtn.disabled=true;loginBtn.textContent="Signing in...";try{const result=await signInWithPopup(auth,googleProvider);await ensureUserDoc(result.user);window.location.href="dashboard.html";}catch(err){console.error("Sign-in failed:",err);const errorEl=document.getElementById("login-error");if(errorEl){errorEl.textContent="Sign-in failed. Please try again.";errorEl.style.display="block";}loginBtn.disabled=false;loginBtn.innerHTML='<img src="assets/google-icon.svg" alt="" /> Continue with Google';}});}
if(document.body.dataset.page==="login")onAuthStateChanged(auth,user=>{if(user)window.location.href="dashboard.html"});
if(document.body.dataset.protected==="true")onAuthStateChanged(auth,user=>{if(!user){window.location.href="index.html";return;}renderUserChrome(user)});
async function ensureUserDoc(user){const ref=doc(db,"users",user.uid);const snap=await getDoc(ref);const data={name:user.displayName||"",email:user.email||"",photoURL:user.photoURL||"",lastLogin:serverTimestamp()};if(!snap.exists()){data.createdAt=serverTimestamp();data.currency="INR";}await setDoc(ref,data,{merge:true});}
function renderUserChrome(user){document.querySelectorAll("[data-user-name]").forEach(el=>el.textContent=user.displayName||"User");document.querySelectorAll("[data-user-email]").forEach(el=>el.textContent=user.email||"");document.querySelectorAll("[data-user-photo]").forEach(el=>el.src=user.photoURL||"assets/default-avatar.svg");document.querySelectorAll("[data-logout-btn]").forEach(btn=>btn.addEventListener("click",async()=>{await signOut(auth);window.location.href="index.html";}));}
export {ensureUserDoc};