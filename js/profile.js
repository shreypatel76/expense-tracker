import {auth,onAuthStateChanged} from "./firebase.js";
import {fetchAllTransactions} from "./transactions.js";
const money=n=>"₹"+Number(n||0).toLocaleString("en-IN",{maximumFractionDigits:2});
onAuthStateChanged(auth,async user=>{if(!user)return;document.getElementById("profile-name").textContent=user.displayName||"User";document.getElementById("profile-email").textContent=user.email||"";document.getElementById("profile-photo").src=user.photoURL||"assets/default-avatar.svg";document.getElementById("profile-joined").textContent=user.metadata?.creationTime?new Date(user.metadata.creationTime).toLocaleDateString("en-IN",{day:"numeric",month:"long",year:"numeric"}):"—";const txs=await fetchAllTransactions(),income=txs.filter(t=>t.type==="income").reduce((s,t)=>s+t.amount,0),expense=txs.filter(t=>t.type==="expense").reduce((s,t)=>s+t.amount,0);document.getElementById("profile-total-tx").textContent=txs.length;document.getElementById("profile-total-income").textContent=money(income);document.getElementById("profile-total-expense").textContent=money(expense)});